# 数据库连接字符串
# 本地开发 (SQLite): file:./database/dev.db
# 生产环境 (PostgreSQL/MySQL): postgresql://user:password@host:port/dbname
DATABASE_URL="file:./database/dev.db"

# 服务端口 (Vercel 会自动处理，本地开发默认 3000)
PORT=3000

# 如果你未来加入了登录功能，需要在这里设置密钥
# JWT_SECRET=your_super_secret_key_here
